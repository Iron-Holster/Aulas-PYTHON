import os
os.system("cls || clear")

#Entrada
notas =[2,5,10] #Vetores ou lista. cada número representa uma variavel e cada posição rpresenta seu indice.

#Saida
#for i in range(?): #len() descobre o tamanho do vetor

#for each. descobre o tamanho da lista e percorre por cada posição
for nota in notas: #for it. para cada nota em notas, me mostre cada uma
    print(nota)

#por índice
print(notas[1]) #vai imprimir a nota na "2" posição (computador conta a partir do zero)
