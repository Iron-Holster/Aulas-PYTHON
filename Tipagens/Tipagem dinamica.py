import os

os.system("cls || clear") #Limpa o console
# escreva (git add .) no terminal e (git commit -m "nome do salvamento") para passar as alterações para o Github e finalize com (git push)
# Barra reta dupla = OU (||)

# "==" significa igualdade entre 2 coisas, já "!=" representa a difereça entre 2 coisas

.upper() #transforma qualquer letra digitada pelo usuario em maiuscula

#Na tipagem dinamica não é ditada os tipos das variaveis, como Real ou Cadeia
nome = "Mario"
idade = 42
altura = 1.72

print(f"Nome: {nome}")
print(f"Idade: {idade}")
print(f"Altura: {altura}")

#Vantagens: Melhor para pequenos programas
#Contras: Menos organizado em projetos grandes